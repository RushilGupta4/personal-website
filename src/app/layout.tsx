import './globals.css'
import type { Metadata } from 'next'
import Link from 'next/link'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
}

function NavItem({ href, children }: { href: string; children: React.ReactNode }) {
  return (
    <li className={`link link-underline link-underline-black pb-2`}>
      <Link href={href} className={`text-white font-extrabold text-center text-md sm:text-lg px-2`}>{children}</Link>
    </li>
  )
}

function NavBar() {
  return (
    <div className={`bg-gradient-to-b from-[#00BDFE] to-[#fe7987] backdrop-blur-sm pb-10 sm:pb-[72px] px-4 sm:px-16 md:px-20 lg:px-32`}>
      <ul className={`flex items-center justify-center pt-4 sm:justify-start gap-x-4 sm:gap-x-12`}>
        <NavItem href="/">Home</NavItem>
        <NavItem href="/blogs">Blogs</NavItem>
      </ul>
      <p className={`pt-14 sm:pt-16 text-[38px] leading-none sm:text-[56px] md:text-[64px] lg:text[72px] text-white font-semibold`}>Student Developer</p>
      <p className={`text-[18px] sm:text-[20px] text-white font-light sm:font-extralight`}>Ashoka University</p>
    </div>
  ) 
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      
      <body className={`${inter.className} bg-gray-100 text-black mx-auto`}>
      <NavBar />
      <div className={`px-4 sm:px-16 md:px-20 lg:px-32`}>{children}</div>
      </body>
    </html>
  )
}
